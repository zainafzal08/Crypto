this program  reads in a set of hashes from hashes.txt and 
cracks all of them assuming single char XOR encryption.
will output the decryptions that are most likey to be messages.

to Compile: gcc -Wall -Werror -o run XOR.c